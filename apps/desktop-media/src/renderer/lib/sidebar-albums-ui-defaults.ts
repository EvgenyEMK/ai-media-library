/**
 * UI defaults for the Albums sidebar panel (`DesktopSidebarAlbumsSection`).
 * When app settings include “expand RECENT by default”, pass the
 * `expandRecentAlbumsByDefault` prop from the parent (overrides this default).
 */
export const DEFAULT_SIDEBAR_ALBUMS_EXPAND_RECENT = false;
