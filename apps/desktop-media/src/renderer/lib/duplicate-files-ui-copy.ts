/**
 * UI copy for duplicate-folder views. Kept as separate string fragments for future i18n.
 */

/** Outside-duplicate section heading when the selection includes subfolders (disk tree). */
export const DUPLICATE_FILES_TITLE_OUTSIDE_TREE = "Duplicates outside selected folder tree";

/** Outside-duplicate section heading when the selection has no subfolders in this breakdown. */
export const DUPLICATE_FILES_TITLE_OUTSIDE_FLAT = "Duplicates outside selected folder";

/** Inside-duplicate section heading when the selection includes subfolders (disk tree). */
export const DUPLICATE_FILES_TITLE_INSIDE_TREE = "Duplicates within selected folder tree";

/** Inside-duplicate section heading when the selection has no subfolders in this breakdown. */
export const DUPLICATE_FILES_TITLE_INSIDE_FLAT = "Duplicates within selected folder";
