# web-media status

This folder is intentionally a placeholder in Phase 1.

The web-media app is currently maintained in a separate repository and is not published in `ai-media-library` yet.

When web-media is added here in a future phase, it will consume the shared packages from this monorepo (`media-store`, `media-viewer`, `shared-contracts`, `sdk-media-api`).
